# Restlicher Key-Satz fehlt bewusst — btop initialisiert alles andere mit
# eingebauten Defaults, nur der Theme-Name muss auf die Datei in themes/ zeigen.
color_theme = "catppuccin_mocha"
theme_background = True
